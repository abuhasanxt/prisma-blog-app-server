import { number } from "better-auth/*";

type IOptions={
    page?:number |string,
    limit?:number|string,
    sortBy?:string,
    sortOrder:string

}

const paginationSortingHelper=(options:IOptions)=>{

}